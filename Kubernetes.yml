apiVersion: v1
kind: Service
metadata:
  name: expense-service
spec:
  type: NodePort
  selector:
    app: expense
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: expense-tracker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: expense
  template:
    metadata:
      labels:
        app: expense
    spec:
      containers:
        - name: expense
          image: ghcr.io/ahmedkhafagy6/expense-tracker:0c1a8f1ec559af82830299993e57dff54afcc083
          ports:
            - containerPort: 5000